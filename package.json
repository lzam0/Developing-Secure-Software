{
  "name": "developing-secure-software",
  "private": true,
  "type": "module",
  "scripts": {
    "db": "npm --prefix backend run db",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
