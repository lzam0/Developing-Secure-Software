services:
  cv-db:
    image: postgres:16
    container_name: health-blog-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: health-blog-db
    ports:
      - "5433:5432"
    volumes:
      - ./backend/database/init.sql:/docker-entrypoint-initdb.d/01-init.sql
      - ./backend/database/add-post.sql:/docker-entrypoint-initdb.d/02-add-post.sql
    networks:
      - data-system-network

networks:
  data-system-network:
    driver: bridge
